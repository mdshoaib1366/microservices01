server:
  port: 8084

spring:
  application:
    name: user

management:
  tracing:
    enabled: true
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      enabled: true
      base-url: http://localhost:9411
